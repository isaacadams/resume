name: test

on:
  workflow_run:
    workflows:
      - resume build
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo Hello World
